apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: writefreely-app-pvc
spec:
  accessModes: [ReadWriteOnce]
  resources: {requests: {storage: 1Gi}}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: writefreely-db-pvc
spec:
  accessModes: [ReadWriteOnce]
  resources: {requests: {storage: 2Gi}}
